<?xml version="1.0"?>
<odoo>
    <record id="estate_properties_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_available': True}</field>
    </record>

    <record id="estate_properties_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties"
                decoration-success="state in ['offer_received', 'offer_accepted']"
                decoration-bf="state == 'offer_accepted'" decoration-muted="state == 'sold'">
                <field name="title" width="150px" />
                <field name="postcode" width="50px" />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="expected_price" />
                <field name="selling_price" />
                <field name="date_availability" optional="True" />
            </list>
        </field>
    </record>

    <record id="estate_properties_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button string="Sold" name="action_set_property_as_sold" type="object"
                        class="oe_highlight" invisible="state == 'sold'or state == 'cancelled'" />
                    <button string="Cancel" name="action_set_property_as_cancelled" type="object"
                        class="oe_highlight" invisible="state == 'sold'or state == 'cancelled'" />

                    <field name="state" widget="statusbar"
                        statusbar_visible="new,offer_received,offer_accepted,sold" />
                </header>
                <sheet>
                    <group>
                        <h1>
                            <field name="title" />
                        </h1>
                    </group>
                    <field name="property_tag_ids" widget="many2many_tags"
                        options="{'color_field': 'color'}" />
                    <group>
                        <group>
                            <field name="property_type_id" options="{'no_create': true}" />
                            <field name="postcode" />
                            <field name="date_availability" />
                        </group>
                        <group>
                            <field name="expected_price" />
                            <field name="best_offer" />
                            <field name="selling_price" />
                        </group>
                    </group>

                    <notebook>
                        <page name="description" string="Description">
                            <!-- If you don't put fields inside of groups, their labels won't show -->
                            <group>
                                <field name="description" />
                                <field name="bedrooms" />
                                <field name="living_area" />
                                <field name="facades" />
                                <field name="garage" />
                                <field name="garden" />
                                <field name="garden_area" invisible="not garden" />
                                <field name="garden_orientation" invisible="not garden" />
                                <field name="total_area" />
                            </group>
                        </page>
                        <page name="offers" string="Offers">
                            <field name="offer_ids"
                                readonly="state in ['offer_accepted', 'sold', 'cancelled']" />
                        </page>
                        <page name="other_info" string="Other Info">
                            <group>
                                <field name="salesman_id" />
                                <field name="buyer_id" />
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_properties_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="title"></field>
                <field name="postcode"></field>
                <field name="expected_price"></field>
                <field name="bedrooms"></field>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"
                    string="Minimum Living Area"></field>
                <field name="facades"></field>
                <field name="property_type_id" />
                <field name="property_tag_ids" />

                <filter name="available" string="Available"
                    domain="['|', ('state', '=', 'new'), ('state', '=', 'offer_received')]" />

                <group>
                    <filter name="postcode" string="Postcode" context="{'group_by': 'postcode'}" />
                </group>

                <group>
                    <filter name="property_type" string="Property Type"
                        context="{'group_by': 'property_type_id'}" />
                </group>
            </search>
        </field>
    </record>
</odoo>